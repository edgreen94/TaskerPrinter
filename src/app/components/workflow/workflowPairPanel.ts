/** Shared shell for the sample ticket + expectations pair (must stay in sync). */
export const workflowPairPanelClass =
  "flex h-full min-h-0 flex-col rounded-2xl border border-border/90 bg-card p-6 sm:p-8 shadow-sm ring-1 ring-black/[0.02]";
